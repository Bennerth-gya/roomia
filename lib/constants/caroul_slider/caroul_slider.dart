import 'package:flutter/material.dart';

class CaroulSlider extends StatelessWidget {
  const CaroulSlider({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
